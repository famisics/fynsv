# claude-remote の serial:ix2215 ターゲット定義。
# parse-target.sh から `source` される。

# device は glob 可。USB ポート由来のサフィックス (…1410 / …130 等) を吸収するため
# PL2303G-USBtoUART* で一致させ、parse-target.sh が実デバイスへ解決する。
# 複数一致した場合はあいまいとしてエラーになる。
device=/dev/cu.PL2303G-USBtoUART*
baud=9600
