# prefix のあと Ctrl を押したまま d を打っても (= C-d) デタッチする。
# 既定では prefix 後の C-d は未割り当てでペインに素通りし、claude が
# 終了してしまうため、誤爆を「終了」ではなく「デタッチ」に倒す。
bind C-d detach-client

# マウス操作とスクロールバックを有効化
set -g mouse on
set -g history-limit 50000
