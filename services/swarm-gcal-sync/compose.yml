services:
  sync:
    build:
      context: .
    env_file:
      - .env
    volumes:
      - ./credentials.json:/secrets/credentials.json:ro
      - ./foursquare-token.json:/secrets/foursquare-token.json:ro
    restart: unless-stopped
